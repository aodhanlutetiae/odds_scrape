    # - name: Add and commit	
    #   run: |-	
    # git add .	
    # git config user.name "Automated"	
    # git config user.email "actions@users.noreply.github.com"	
    # git commit -m "Latest data" 	

    this was the original commit code before we fixed the 'can't commit if there's nothing new' problem